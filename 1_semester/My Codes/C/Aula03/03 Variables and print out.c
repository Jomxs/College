#include <stdio.h>

int main (){

    int age = 25;
    printf("Age: %d", age);
    //%d: is a format specifier which acts as a placeholder in a input and output statement.

    age = 38;
    printf("\nNew age: %d",age);
    // \n: new line.

    //You CANNOT create variables with: space between, start with a number and certain words as "if".

return 0;
}

