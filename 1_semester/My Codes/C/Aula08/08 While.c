#include <stdio.h>

 int main ()
{
    int counter;

    while (counter <= 5){

        printf ("%i\n", counter);
        //$i = print the amount of loops it did
        ++counter;
        // while = does something while the value is correct, if it's wrong don't.
        //++ means add one to the variable counter when the loop ends
    }

}
