#include <iostream>

using namespace std;

int main () {

    int x = 4 % 6;
    // %: it gets only the remainder of the division
    int y = 6 * (4 + 8) * 4 +9 / 20;
    // it uses arithmetic way of doing equations

    cout << y << endl;

    cout << x;

    return 0;
}
